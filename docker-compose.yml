services:
  app:
    build: .
    container_name: taypa-internal
    working_dir: /app
    volumes:
      - ./app:/app
      - app_node_modules:/app/node_modules
    ports:
      - "${APP_PORT:-5173}:5173"
    stdin_open: true
    tty: true
    command: sleep infinity

volumes:
  app_node_modules:
